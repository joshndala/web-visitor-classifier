import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1995AD',
    },
    secondary: {
      main: '#A1D6E2',
    },
    accent: {
      main: '#F1F1F2',
    },
  },
});

export default theme;